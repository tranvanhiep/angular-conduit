import { NotNullDirective } from './not-null.directive';

describe('NotNullDirective', () => {
  it('should create an instance', () => {
    const directive = new NotNullDirective();
    expect(directive).toBeTruthy();
  });
});
